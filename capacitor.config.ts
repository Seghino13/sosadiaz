import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sosadiaz.demo',
  appName: 'sosadiaz',
  webDir: 'dist/sosadiaz/browser'
};

export default config;
